# Розрахунок ПДВ по елеваторних послугах МХП

## Структура папок

```
mhp/
  calc_pdv.py              ← скрипт (один для всіх місяців)
  CLAUDE.md                ← ця документація
  prices/
    price_пізні_2025-2026.pdf   ← прайс пізніх зернових (оновлюється раз на рік)
    price_ранні_2025-2026.pdf   ← прайс ранніх зернових (оновлюється раз на рік)
  reports/
    2026-03/
      main_report_march2026.xls        ← вхідний звіт
      main_report_march2026_result.xls ← результат (генерує скрипт)
    2026-04/               ← додати наступного місяця
      main_report_april2026.xls
```

## Що робить цей проєкт

Щомісячний звіт `main_report_МІСЯЦЬ.xls` містить 3 вкладки:
- **зберігання** — зберігання зерна (грн/т/день)
- **сушка** — сушка та очистка (грн/т/%)
- **приймання** — приймання зерна (грн/т)

Скрипт `calc_pdv.py`:
1. Бере з прайс-листів суму ПДВ по кожній послузі/організації/культурі
2. Множить на кількість (Дельта або ФизВес)
3. Записує колонки `ціна ПДВ` та `Сума ПДВ` у кожну вкладку
4. Створює 4-ту вкладку **Зведений звіт**: скільки кожна організація виставить кожному контрагенту
5. Переміщення між філіями (де org ≈ контрагент) виводяться окремим блоком

## Як запустити

```bash
# 1. Створи папку для нового місяця і поклади туди файл звіту
#    reports/2026-04/main_report_april2026.xls

# 2. Запусти скрипт:
python3 calc_pdv.py reports/2026-04/main_report_april2026.xls

# Результат з'явиться автоматично поруч:
# reports/2026-04/main_report_april2026_result.xls
```

## Структура вхідного файлу

**Важливо:** індекси колонок НЕ зашиті в скрипт. Вигрузка з 1С «пливе» від місяця
до місяця (змінюється ширина об'єднаних комірок), тому скрипт щоразу знаходить
рядок заголовків і визначає колонки **за їхніми назвами** (`_find_header_row`,
`_header_map`, `_col` у `calc_pdv.py`). Вкладки теж беруться за іменем, а не за
порядковим номером — порядок вкладок у файлі може бути будь-який.

Приклад того, як колонки зсувались між місяцями (та сама вигрузка):

| Вкладка | Поле | Травень 2026 | Серпень 2026 |
|---------|------|--------------|--------------|
| зберігання | Культура | 13 | 15 |
| зберігання | ІНН орг. / контрагента | 6 / 16 | 7 / 17 |
| приймання | Культура | 14 | 15 |
| приймання | ФизВес | 26 | 24 |
| сушка | Реальная номенклатура | 11 | 13 |
| сушка | Количество_очистка / сушка | 17 / 25 | 19 / 24 |

### Вкладка `зберігання`
Рядок заголовків — той, де є `Организация`, `Контрагент`, `Культура`,
`Кількість зберігання т/д`. Дані — з наступного рядка.
ІНН організації — перший `ИНН` правіше `Организация`; ІНН контрагента — перший
`ИНН` правіше `Контрагент`.
Результат дописується у **дві нові колонки в кінці вкладки**: `ціна ПДВ`, `Сума ПДВ`.

**Формула:** `Сума = Кількість зберігання т/д × ціна ПДВ`

### Вкладка `сушка`
Заголовки у **двох рядках**: верхній (`Организация`, `Контрагент`,
`Реальная номенклатура`, `ИНН`) і нижній (`Количество_очистка_т%`,
`Количество_сушка_т%`). Дані — з рядка `заголовок + 2`.
Результат — **чотири нові колонки в кінці**: `ціна/Сума ПДВ очистка`,
`ціна/Сума ПДВ сушка`.

**Формула:** `Сума очистка = (Количество_очистка_т% / 1000) × ціна ПДВ очистка`
**Формула:** `Сума сушка   = (Количество_сушка_т% / 1000) × ціна ПДВ сушка`

### Вкладка `приймання`
Рядок заголовків — той, де є `Организация`, `ОтправительПолучатель`, `Культура`,
`ФизВес`. Дані — з наступного рядка.
Результат — **дві нові колонки в кінці**: `цінаПДВ`, `Сума ПДВ`.

**Формула:** `Сума = ФизВес × ціна ПДВ`

### Якщо формат зміниться сильніше
Скрипт впаде з явним повідомленням виду
`Вкладка "сушка": не знайдено колонку "количество_сушка_т%"` —
тоді треба оновити назви заголовків у відповідній секції `process()`.

## Прайс-листи

### Пізні зернові (`prices/price_пізні_2025-2026.pdf`) — діє з 01.10.2025
Культури: Кукурудза/сорго, Соняшник, Соя.

| Організація в звіті | TE | Ключові слова для матчингу |
|--------------------|----|---------------------------|
| ВКВК філія ТОВ "Вінницька птахофабрика" | TE-18 | `вквк`, `птахофабрика` |
| Катеринопільський елеватор ТОВ | TE-17 | `катеринопільськ` |
| МЗВКК ПрАТ | TE-16 | `мзвкк` |
| МХП-Андріяшівський елеватор | TE-19 | `андріяшів`, `урожайна` |
| Ямпільський елеватор | TE-20 | `ямпільськ` |
| Вендичанський елеватор | TE-21 | `вендичанськ`, `агрокряж` |
| Елеваторний комплекс (Соколівський/Шпиківський/Калинівський) | TE-22/23/30 | `елеваторний` |
| Воскресинцівський елеватор | TE-24 | `воскресинц` |
| Львівський елеватор | TE-25 | `львівськ` |
| Краснянський елеватор | TE-26 | `краснянськ` |
| Новомосковська філія Оріль-Лідер | TE-27/28/29 | `новомосковськ` |
| Яготинський елеватор | TE-30 | `яготинськ`, `агро-с` |
| Городенківський ел-тор ф-я Перспектив | TE-31 | `перспектив` |

### Ранні зернові та олійні (`prices/price_ранні_2026-2027.pdf`) — діє з 01.08.2026
Культури: Пшениця/ячмінь/жито/овес (ранні зернові), Ріпак.

**Увага: з 2026 МР структура прайсу змінилась.** До цього був ОДИН спільний
прайс ТЕ-03 (ВКВК) на всі організації. Тепер у кожного елеватора свій прайс-лист,
тому `EARLY_GRAIN_PRICES` має таку саму структуру, що й `PRICES` — розріз по
організаціях. Попередній прайс лишився в репо як `price_ранні_2025-2026.pdf`.

| ТЕ | Організація | Ключ матчингу |
|----|-------------|---------------|
| ТЕ-01 | ПрАТ "МЗВКК" | `мзвкк` |
| ТЕ-02 | ТОВ "Катеринопільський елеватор" | `катеринопільськ` |
| ТЕ-03 | ВКВК ф-я ТОВ "Вінницька птахофабрика" | `вквк`, `птахофабрика` |
| ТЕ-04 | ф-я "МХП-Андріяшівський елеватор" | `андріяшів`, `урожайна` |
| ТЕ-05 | Ямпільський елеватор | `ямпільськ` |
| ТЕ-06 | Вендичанський елеватор | `вендичанськ`, `агрокряж` |
| ТЕ-07 / 08 / 13 | Елеваторний комплекс: Соколівський / Шпиківський / Калинівський | `елеваторний` |
| ТЕ-09 | Воскресинцівський елеватор | `воскресинц` |
| ТЕ-10 | Львівський елеватор | `львівськ` |
| ТЕ-11 | Краснянський елеватор | `краснянськ` |
| ТЕ-12 / 12.1 / 12.2 | Оріль-Лідер: Китайгород / Меліоративне / Рокита | `новомосковськ` |
| ТЕ-14 | Яготинський елеватор | `яготинськ`, `агро-с` |
| ТЕ-15 | Городенківський ел-тор ф-я Перспектив | `перспектив` |

### Де один ключ покриває кілька прайс-листів
`елеваторний` і `новомосковськ` відповідають трьом прайс-листам кожен, бо у
вигрузці з 1С усі майданчики приходять одним рядком під спільною назвою та
спільним ІНН — розрізнити їх неможливо. Рішення від 24.09.2026: брати
**максимум ціни по кожній позиції** з відповідних прайс-листів.

## Позиції, яких немає у прайсах

Головний принцип: **нова позиція ніколи не дає мовчазний 0**. Вона рахується
за запасною ціною і обовʼязково потрапляє у вкладку «Попередження», щоб
бухгалтер побачив її і перевірив вручну.

| Ситуація | Що застосовується | Попередження |
|----------|-------------------|--------------|
| Нова культура, якої немає в прайсі | ціни `DEFAULT_CROP_KEY` (ріпак) того самого елеватора | Невідома культура |
| Нове підприємство, якого немає в прайсі | прайс `DEFAULT_ORG_KEY` (Ямпільський, ТЕ-05) | Невідома організація |
| Культура з домовленим аналогом (`CROP_ANALOG`) | ціни культури-аналога | Ціна за аналогією |
| Організація з домовленим тарифом (`ORG_ANALOG`) | прайс домовленої організації | Ціна за аналогією |

Різниця між двома парами: `DEFAULT_*` — це автоматична підстраховка для того,
чого ми ще не бачили. `*_ANALOG` — свідоме рішення по конкретній позиції.

### Чинні домовленості

| Позиція | Рахується за | Рішення |
|---------|--------------|---------|
| Гірчиця | ріпак того самого елеватора | 24.09.2026 |
| «Урожай НВФ ТОВ» | Ямпільський елеватор (ТЕ-05) — філія ТОВ "Зернопродукт МХП" | 24.09.2026 |

Коли позиція отримує власний тариф — прибрати її з `CROP_ANALOG` / `ORG_ANALOG`
і додати у `PRICES` або `EARLY_GRAIN_PRICES`.

### Що все одно дасть 0
Якщо культура є в прайсі, але у `DEFAULT_ORG_KEY` для неї ціни немає.
Приклад: нове підприємство + соняшник — у Ямпільського в пізньому прайсі
соняшнику немає, тож буде 0 з попередженням «Відсутня ціна».

### Гірчиця
Власного прайсу не має **в жодному прайс-листі** — ні в ранніх, ні в пізніх,
ні до 2026 МР, ні після. Рішення від 24.09.2026: рахувати за цінами **ріпаку**
того самого елеватора (`CROP_ANALOG = {'гірчиця': 'ріпак'}`) з попередженням
«Ціна за аналогією». Коли зʼявиться власний тариф — прибрати з `CROP_ANALOG`
і додати `'гірчиця'` у `EARLY_GRAIN_PRICES` поруч із `'ріпак'`.

## Що оновлювати щороку / щомісяця

### Щомісяця — нічого, просто запускай:
```bash
python3 calc_pdv.py reports/main_report_МІСЯЦЬ2026.xls
```

### При зміні прайс-листів (новий маркетинговий рік):
Поклади новий PDF у `prices/` і онови у `calc_pdv.py`:
- `PRICES` — пізні зернові (кукурудза/сорго, соняшник, соя),
- `EARLY_GRAIN_PRICES` — ранні зернові та ріпак.

Формат значень: `ціна_з_ПДВ − ціна_без_ПДВ` (беруться округлені до копійки
значення з PDF). Не забудь про `CROP_ANALOG` — якщо гірчиця отримає власний
тариф, її треба звідти прибрати.

### При зміні формату звіту:
Зазвичай **нічого робити не треба** — колонки шукаються за назвами заголовків.
Втручання потрібне, лише якщо в 1С перейменували саму колонку: тоді скрипт впаде
з повідомленням `Вкладка "...": не знайдено колонку "..."`, і треба оновити
назву у відповідній секції `process()`.

## Переміщення між філіями

Якщо організація та контрагент належать до одного юридичного суб'єкта — вважається внутрішнім переміщенням:
- ВКВК ↔ Вінницька птахофабрика
- Вендичанський ↔ МХП-Агрокряж
- МХП-Андріяшівський ↔ МХП-Урожайна Країна
- Яготинський ↔ МХП-Агро-С
- Городенківський ↔ Перспектив
- Захід-Агро МХП (Воскресинцівський / Львівський / Краснянський) — між собою

Визначається автоматично: якщо **ІНН організації = ІНН контрагента** — це переміщення.
Ці рядки виводяться червоним у блоці "ПЕРЕМІЩЕННЯ МІЖ ФІЛІЯМИ" і не входять у основний підсумок.

## Вкладка «Попередження» у результаті

| Тип | Що означає | Сума |
|-----|-----------|------|
| Невідома культура | Культури немає в прайсі → взято ціни `DEFAULT_CROP_KEY` (ріпак) | розрахована, перевірити |
| Невідома організація | Підприємства немає в прайсі → взято прайс `DEFAULT_ORG_KEY` (Ямпільський) | розрахована, перевірити |
| Ціна за аналогією | Домовленість із `CROP_ANALOG` (гірчиця) або `ORG_ANALOG` (Урожай НВФ) | розрахована, перевірити |
| Відсутня ціна | Ціни немає навіть у прайсі, взятому за замовчуванням | 0 |

## Залежності Python

```bash
pip3 install xlrd openpyxl xlwt xlutils
```
